<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.users.UsersDAO">
	<insert id="register" parameterType="UsersDTO">
		INSERT INTO users VALUES(#{username}, #{password}, #{name}, #{email}, #{phone}, #{birth})
	</insert>
	
	<insert id="userFileAdd" parameterType="UsersFileDTO">
		INSERT INTO profile VALUES(null, #{fileName}, #{fileOrigin}, #{username});
	</insert>
	
	<select id="detail" parameterType="UsersDTO" resultMap="detailResult">
		SELECT * FROM users U LEFT JOIN profile P using (username) WHERE username=#{username};
	</select>
	
	<resultMap type="UsersDTO" id="detailResult">
		<id column="username" property="username"/>
		<result column="password" property="password"/>
		<result column="name" property="name"/>
		<result column="email" property="email"/>
		<result column="phone" property="phone"/>
		<result column="birth" property="birth"/>
		<association property="usersFileDTO" javaType="UsersFileDTO">
			<id column="file_num" property="fileNum"/>
			<result column="file_name" property="fileName"/>
			<result column="file_origin" property="fileOrigin"/>
			<result column="username" property="username"/>
		</association>
	</resultMap>
</mapper>