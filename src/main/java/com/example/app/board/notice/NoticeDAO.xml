<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.app.board.notice.NoticeDAO"> 
	<!-- 반복되는 쿼리를 모아놓은 것 -->
	<sql id="search">
		<if test="kind == 'k1'">
			WHERE board_title LIKE CONCAT('%',#{search},'%')
		</if>
		<if test="kind == 'k2'">
			WHERE board_contents LIKE CONCAT('%',#{search},'%')
		</if>
		<if test="kind == 'k3'">
			WHERE board_writer LIKE CONCAT('%',#{search},'%')
		</if>
	</sql>
	
	<select id="count" parameterType="Pager" resultType="Long">
		SELECT COUNT(board_num) FROM notice
		<include refid="search"></include>
	</select>
	
	<select id="detail" parameterType="NoticeDTO" resultType="NoticeDTO">
		SELECT * FROM notice WHERE board_num=#{boardNum}
	</select>
	
	<select id="list" parameterType="Pager" resultType="NoticeDTO">
		SELECT * FROM notice 
		<include refid="search"></include>
		ORDER BY board_num DESC LIMIT #{startNum}, #{perPage}
	</select>
	
	<insert id="add" parameterType="NoticeDTO" useGeneratedKeys="true" keyProperty="boardNum">
		INSERT INTO notice VALUES (null, #{boardTitle}, #{boardWriter}, #{boardContents}, now(), 0)
	</insert>
	
	<update id="update" parameterType="NoticeDTO">
		UPDATE notice SET board_title=#{boardTitle}, board_contents=#{boardContents} WHERE board_num=#{boardNum}
	</update>
	
	<delete id="delete" parameterType="NoticeDTO">
		DELETE FROM notice WHERE board_num=#{boardNum}
	</delete>
	
	<insert id="fileAdd" parameterType="NoticeFileDTO">
		INSERT INTO notice_files VALUES (null, #{fileName}, #{fileOrigin}, #{boardNum})
	</insert>
</mapper>